{
  "name": "donatello",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run setup:db:test && npx jest --runInBand --detectOpenHandles",
    "test:watch": "npm run setup:db:test && npx jest --watch --runInBand --detectOpenHandles",
    "setup:db:test": "NODE_ENV=test npm run migrate:undo:all && NODE_ENV=test npm run migrate",
    "migrate:undo:all": "npx sequelize db:migrate:undo:all",
    "migrate": "npx sequelize db:migrate",
    "db:reset": "npm run migrate:undo:all && npm run migrate && npx sequelize db:seed:all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mollie/api-client": "^3.5.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "faker": "^5.5.3",
    "jest": "^26.6.3",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.6.0",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.6.2",
    "supertest": "^6.1.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.2.0"
  }
}
